## Devtools Part 2
1. The bug is that the 2 input numbers are strings so it concatenates the two numbers instead of adding them
2. I would fix it by getting the numerical values of the inputs then adding them and returning tht result